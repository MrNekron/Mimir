<<<<<<< HEAD
name: Build Mimir App

on:
  push:
    branches: [ main ]

jobs:
  build:
  
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller customtkinter pandas openpyxl nicegui pywebview

    - name: Build with PyInstaller
      run: |
        pyinstaller --noconsole --onefile --add-data "Структура СМУ.xlsx;." main.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: Mimir-Exe
        path: dist/app.exe
=======
name: Build Mimir App

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller customtkinter pandas openpyxl nicegui pywebview

    - name: Build with PyInstaller
      run: |
        pyinstaller --console --onefile --add-data "Структура СМУ.xlsx;." app.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: Mimir-Exe
        path: dist/app.exe
>>>>>>> de66a23aa08c7c30f6c2b79b69eb2d328529fa0c
